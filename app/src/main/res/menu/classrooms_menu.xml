<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/change_date"
        android:title="@string/change_date"
        app:showAsAction="never"/>

    <item android:id="@+id/force_update"
        android:title="@string/force_update"
        app:showAsAction="never"/>

    <item android:id="@+id/subjects_settings"
        android:title="@string/subjects_settings"
        app:showAsAction="never"/>

</menu>